@model M.Models.AREAS

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>AREAS</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.areaname)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.areaname)
            @Html.ValidationMessageFor(model => model.areaname)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.x_coord)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.x_coord)
            @Html.ValidationMessageFor(model => model.x_coord)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.y_coord)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.y_coord)
            @Html.ValidationMessageFor(model => model.y_coord)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
